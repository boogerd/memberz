<%= form_with(model: membership_type) do |form| %>
  <% if membership_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membership_type.errors.count, "error") %> prohibited this membership_type from being saved:</h2>

      <ul>
        <% membership_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code, class: "label" %>
    <div class="control">
      <%= form.text_field :code, class: "input" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :description, class: "label" %>
    <div class="control">
      <%= form.text_field :description, class: "input" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :price, class: "label" %>
    <div class="control">
      <%= form.text_field :price, class: "input" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :organisation_id, class: "label" %>
    <div class="select">
      <%= form.collection_select :organisation_id, Organisation.all, :id, :name, { prompt: true }  %>
    </div>
  </div>

 <div class="field is-grouped">
    <%= link_to "Back", membership_types_path, class: "button is-info mr-2" %>
    <%= form.submit class: 'button is-primary' %>  
  </div>
<% end %>
